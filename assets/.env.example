# HomeSigSec dashboard runtime env (DO NOT COMMIT real values)

# Where HomeSigSec writes output on the host. Must be an absolute path for docker volume mapping.
HOMESIGSEC_WORKDIR=/ABS/PATH/TO/HomeSigSec/output

# Dashboard bind socket (ip:port). Set to a LAN or Tailscale interface as desired.
# Examples:
# - LAN_HOST_IP:8090
# - TS_HOST_IP:8090
# - 127.0.0.1:8090
HOMESIGSEC_BIND=LAN_HOST_IP:8090

# Target SSID for fingerprinting (your home network name)
HOMESIGSEC_TARGET_SSID="MyHomeNetwork"

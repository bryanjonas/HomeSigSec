# HomeSigSec dashboard runtime env (DO NOT COMMIT real values)

# Where HomeSigSec writes output on the host. Must be an absolute path for docker volume mapping.
HOMESIGSEC_WORKDIR=/ABS/PATH/TO/HomeSigSec/output

# Where HomeSigSec writes output on the host. Must be an absolute path for docker volume mapping.
HOMESIGSEC_WORKDIR=/ABS/PATH/TO/HomeSigSec/output

# Dashboard bind sockets (ip:port). Keep them LAN/Tailscale only.
# If you want the same port on both interfaces, set different IPs but the same port.
HOMESIGSEC_LAN_BIND=LAN_HOST_IP:8090
HOMESIGSEC_TS_BIND=TS_HOST_IP:8090

# HomeSigSec dashboard runtime env (DO NOT COMMIT real values)

# Where HomeSigSec writes output on the host. Must be an absolute path for docker volume mapping.
HOMESIGSEC_WORKDIR=/ABS/PATH/TO/HomeSigSec/output

# Bind addresses for the dashboard web server (LAN/Tailscale only)
# Provide concrete IPs on the host to avoid exposing publicly.
HOMESIGSEC_BIND_LAN=LAN_HOST_IP
HOMESIGSEC_BIND_TS=TS_HOST_IP

# Published ports on the host (can be the same if your OS supports it; safer to keep distinct)
HOMESIGSEC_DASHBOARD_PORT_LAN=8090
HOMESIGSEC_DASHBOARD_PORT_TS=8091

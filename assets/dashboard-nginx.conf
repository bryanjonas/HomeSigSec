server {
  listen 8090;
  server_name _;

  root /www;
  index index.html;

  location /api/ {
    proxy_pass http://homesigsec-api:5000/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }

  location / {
    try_files $uri /index.html;
  }
}
